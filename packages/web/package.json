{
  "name": "@castkit/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Inkcast browser dev-preview — renders every view at each panel's exact pixel size with mock data. Because Chromium is the render engine, this preview matches device output (pre-dither).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "storybook": "storybook dev -p 6006 --no-open",
    "build-storybook": "storybook build",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@castkit/core": "workspace:*",
    "@castkit/views": "workspace:*",
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "devDependencies": {
    "@storybook/addon-docs": "^10.4.6",
    "@storybook/react-vite": "^10.4.6",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.3",
    "storybook": "^10.4.6",
    "typescript": "^6.0.3",
    "vite": "^8.1.2"
  }
}
